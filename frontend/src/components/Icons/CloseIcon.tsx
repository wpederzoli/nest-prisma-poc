import { LoadIcon } from "./LoadIcon";
import { GenericIcon } from "./types";

export const CloseIcon = ({ width, height, styles }: GenericIcon) => (
  <LoadIcon src="/x.svg" width={24} height={24} />
);
